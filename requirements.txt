# Runtime dependencies for the Streamlit Cloud demo.
# pyproject.toml + uv.lock stay the source of truth for local dev and CI.
# This is the subset the dashboard (app.py) actually imports. The report
# stack (matplotlib, python-docx, fpdf2) and python-dotenv are left out
# because they are only used by the CLI report generators, not the app.
streamlit>=1.59.2
pandas>=3.0.3
numpy>=2.4.6
scikit-learn>=1.5.0
scapy>=2.5.0
pydantic>=2.13.4
